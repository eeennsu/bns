import { IBreadItem, IBreadList } from '@entities/bread/types';

export const filterBreadResponse = (response: any): IBreadItem => {
  return response;
};

export const filterBreadListResponse = (response: any): IBreadList => {
  return response;
};
