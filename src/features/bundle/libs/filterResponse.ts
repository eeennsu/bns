import { IBundleItem, IBundleList } from '@entities/bundle/types';

export const filterBundleResponse = (data: any): IBundleItem => {
  return data;
};

export const filterBundleListResponse = (data: any): IBundleList => {
  return data;
};
