import { IEventItem, IEventList } from '@entities/event/types';

export const filterEventResponse = (data: any): IEventItem => {
  return data;
};

export const filterEventListResponse = (data: any): IEventList => {
  return data;
};